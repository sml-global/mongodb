apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# dev keeps the same namespace (signoz) and sourceRef base already uses --
# this overlay exists for structural parity with uat and to make the
# environment-aware selection in scripts/provision-k8s-components.sh
# explicit, not because dev's behavior changes. See
# gitops/operators/overlays/dev/kustomization.yaml for the same pattern.
